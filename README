Installation:
    
    git clone git://github.com/rotajota/myvim.git ~/.vim

Create Symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

Switch to .vim directory and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
